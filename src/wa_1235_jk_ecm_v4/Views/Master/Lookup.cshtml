@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
@model wa_1235_jk_ecm_v4.Models.Master
@{
	ViewData["Title"] = "ValueStream";
	Layout = "~/Views/Shared/_ECMMaster.cshtml";
}


<div class="content-wrapper">
	<!-- InstanceBeginEditable name="EditRegion1" -->


	 <section class="content-header">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-3">
                    <h5>Lookup</h5>
                </div>
                <div class="col-lg-9">
                    <ol class="breadcrumb float-sm-right">
                          <li class="breadcrumb-item"><a href="#" onclick="UserDetailChange('Index')">Home</a></li>
                        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">ECM</a></li>
                      <li class="breadcrumb-item active">Masters</li>
                        <li class="breadcrumb-item active">
                            Lookup
                        </li>
                    </ol>
                </div>
            </div>

            <div class="row">
                <div class="col-lg-12">
                    <div class="btnwidth50 float-right ">
                        <button type="button" class="btn btn-block bg-gradient-danger btn-xs" onclick="window.history.back()" title="Go back to the previous page">
                            Back
                        </button>
                    </div>
                    <div class="btnwidth50 float-right mr-1">
                       <button type="button"
        class="btn btn-block bg-gradient-primary btn-xs float-left"
        title="Add new record"
        onclick="location.href='@Url.Action(
            "AddLookup",
            "Master",
            new {
                PageID = Context.Request.Query["PageID"].ToString(),
                ParentID = Context.Request.Query["ParentID"].ToString(),
                ModuleID = Context.Request.Query["ModuleID"].ToString(),
                ModulePageID = Context.Request.Query["ModulePageID"].ToString()
            })'">
    Add
</button>

                    </div>
                </div>
            </div>
        </div><!-- /.container-fluid -->

    </section>
	<section class="content">
		<div class="container-fluid">
			<div class="row">
				<div class="col-md-12">

					<div class="card card-outline card-primary">
						<!-- /.card-header -->
						<div class="card-body">


							<div>

								<div class="table-responsive">
									<table class="table table-bordered example1 nowrap" style="width:100%;">
										<thead>

											<tr>
												<th width="8%" style="text-align:center;">Sr No.</th>

												<th width="15%"><div align="left">Product Line</div></th>
												<th width="20%"><div align="left">Lookup Type</div></th>
												<th width="25%"><div align="left">Lookup Value&nbsp;</div></th>
												<th width="25%"><div align="left">Image File Name</div></th>
												<th width="8%"><div align="center">Delete</div></th>
											</tr>
										</thead>
										<tbody>
                                            @{
                                                int srno = 1;
                                            }

                                            @if (Model.LookUp_MasterData != null && Model.LookUp_MasterData.Any())
                                            {
                                                foreach (var item in Model.LookUp_MasterData)
                                                {
                                                    <tr>
                                                        <td><div align="center">@srno</div></td>

                                                        <td>
                                                            <div align="left">@item.ProductLine</div>
                                                        </td>

                                                        <td>
                                                           
                                                                @item.LookupType
                                                           
                                                        </td>

                                                        <td>
                                                            <div align="left">@item.LookupValue</div>
                                                        </td>

                                                        <td>
                                                            @if (!string.IsNullOrEmpty(item.ImageFileName))
                                                            {
                                                                <a href="#" onclick="OpenDocument('@item.ImageFileName')"
                                                                  >
                                                                    @item.ImageFileName
                                                                </a>
                                                            }
                                                        </td>

                                                        <td>
                                                            <div style="margin-left:auto; margin-right:auto; float:none; display:table;">
                                                                <div align="center">
                                                                    <a href="javascript:void(0);"
                                                                       onclick="DeleteLookup(@item.LookupId)"
                                                                       class="btn btn-block bg-gradient-danger mt-1 btn-xs float-none">
                                                                        Delete
                                                                    </a>
                                                                </div>
                                                            </div>
                                                        </td>
                                                    </tr>

                                                    srno++;
                                                }
                                            }
                                            else
                                            {
                                                <tr>
                                                    <td colspan="6" class="text-center">No Data Available</td>
                                                </tr>
                                            }
                                        </tbody>
									</table>
								</div>
							</div>


						</div>

					</div>
					<!-- /.card-body -->
				</div>
				<!-- /.card -->
			</div>

			<div class="modal fade" id="ApprovalList">
				<div class="modal-dialog modal-lg">
					<div class="modal-content">
						<div class="modal-header">
							<h5 class="modal-title"> View Approval List </h5>
							<button type="button" class="close" data-dismiss="modal" aria-label="Close">
								<span aria-hidden="true">&times;</span>
							</button>
						</div>
						<div class="modal-body">


							<div class="table-responsive">
								<table class="table table-bordered example1 nowrap" style="width:100%;">
									<thead>

										<tr>
											<th width="2%" style="text-align:center;">Sr.No.</th>

											<th width="103"><div align="center">Stage ID</div></th>
											<th width="314"><div align="left">Stage Name</div></th>
											<th width="335"><div align="left">Approval Name</div></th>
											<th width="184"><div align="left">Role</div></th>
											<th width="184"><div align="center">Approval Type</div></th>
											<th width="66"><div align="center">Sequence</div></th>
										</tr>
									</thead>
									<tbody>
										<tr>
											<td><div align="center">1</div></td>
											<td><div align="center">0</div></td>

											<td><div align="left">stage 1</div></td>
											<td><div align="left">	Akhil Kulkarni(Admin2)</div></td>
											<td><div align="left">Admin2</div></td>

											<td><div align="center">3</div></td>
											<td><div align="center">1</div></td>

										</tr>


									</tbody>
								</table>
							</div>


						</div>
						<div class="modal-footer justify-content-between">
							<button type="button" class="btn btn-default btn-xs" data-dismiss="modal">Close</button>

						</div>
					</div>
					<!-- /.modal-content -->
				</div>
				<!-- /.modal-dialog -->
			</div>
		</div>


		<!-- /.content -->

	</section>

	<!-- InstanceEndEditable -->
</div>
<script>
 async function OpenDocument(FileName, ImageId, ProdLine) {

    var JwToken = '@ViewBag.JWTToken';
    var blobStorageUrl = '@ViewBag.ApiUrl';
    let blobPathPrefix = '@ViewBag.BaseBlobPath';

    var path = 'others\\lookup' + '\\';

    const blobpath =
        (normalizePathPrefix(blobPathPrefix) +
            normalizePathPrefix(path))
            .toLowerCase() + FileName.toLowerCase();

    const JSONData = {
        blobPath: blobpath,
        asSas: false,
        downloadFileName: FileName
    };

    try {

        $('#loader').show();

        const response = await $.ajax({
            url: blobStorageUrl + 'Files/get',
            headers: { Authorization: 'Bearer ' + JwToken },
            type: "POST",
            contentType: "application/json",
            data: JSON.stringify(JSONData),
            xhrFields: { responseType: 'blob' }
        });

        if (response && response.size > 0) {

            const objectURL = URL.createObjectURL(response);

            // ⭐ Open image in new tab
            window.open(objectURL, '_blank');

            // Optional cleanup after 1 minute
            setTimeout(() => {
                URL.revokeObjectURL(objectURL);
            }, 60000);
        }

    }
    catch (error) {

        console.error("Error:", error);

        alert('Image not available');

    }
    finally {

        $('#loader').hide();

    }
}
    //    async function OpenDocument(FileName, ImageId, BrandCode) {

    //     var JwToken = '@ViewBag.JWTToken';
    //     var blobStorageUrl = '@ViewBag.ApiUrl';
    //     let blobPathPrefix = '@ViewBag.BaseBlobPath';
        
    //     var path = 'others\\lookup'+ '\\' ;

    //     const blobpath =
    //         (normalizePathPrefix(blobPathPrefix) +
    //             normalizePathPrefix(path)).toLowerCase() + FileName;

    //     const JSONData = {
    //         blobPath: blobpath,
    //         asSas: false,
    //         downloadFileName: FileName
    //     };

    //     try {

    //         $('#loader').show();

    //         const response = await $.ajax({
    //             url: blobStorageUrl + 'Files/get',
    //             headers: { Authorization: 'Bearer ' + JwToken },
    //             type: "POST",
    //             contentType: "application/json",
    //             data: JSON.stringify(JSONData),
    //             xhrFields: { responseType: 'blob' }
    //         });

    //         if (response && response.size > 0) {

    //             const objectURL = URL.createObjectURL(response);
    //             document.getElementById(FileName).src = objectURL;

    //         }

    //     } catch (error) {

    //         console.error(error);
    //         document.getElementById(FileName).src = '';

    //     } finally {

    //         $('#loader').hide();

    //     }
    // }

      function EditBrand(LookupId) {

        // Constructing the URL using JavaScript
      var pageID = encodeURIComponent('@Context.Request.Query["PageID"]');
    var parentID = encodeURIComponent('@Context.Request.Query["ParentID"]');
    var moduleID = encodeURIComponent('@Context.Request.Query["ModuleID"]');
    var modulePageID = encodeURIComponent('@Context.Request.Query["ModulePageID"]');

    // Construct the full URL
    var url = '@Url.Action("EditLookup", "Master")' +
        '?LookupId=' + encodeURIComponent(LookupId) +
        '&PageID=' + pageID +
        '&ParentID=' + parentID +
        '&ModuleID=' + moduleID +
        '&ModulePageID=' + modulePageID;

    // Redirect to the URL
    window.location.href = url;
    }
     function DeleteLookup(Id) {
        var iLoggedInUserID = parseInt('@Context.Session.GetInt32("CreatedBy")');
        let JsonData = {
            LookupID: Id,
            CreatedDate: moment.utc(new Date()).format(),
            CreatedBy: iLoggedInUserID
        };

        var jsonData = JSON.stringify(JsonData);

        $.ajax({
            type: 'POST',
            url: '@Url.Action("DeleteLookupData", "Master")',
            data: { JsonData: jsonData },
            dataType: 'json',
            success: function (data) {
                alert(data.response);
                window.location.reload();
            },
            error: function (xhr, status, error) {
                console.error('Error occurred:', error);
            }
        });
    }


</script>