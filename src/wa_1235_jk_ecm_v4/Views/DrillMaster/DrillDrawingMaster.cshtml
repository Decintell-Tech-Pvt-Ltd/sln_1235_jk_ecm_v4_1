@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
@model wa_1235_jk_ecm_v4.Models.DrillMaster
@{
	ViewData["Title"] = "DrillDrawingMaster";
	Layout = "~/Views/Shared/_ECMMaster.cshtml";
}
<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
   
    <div class="modal fade" id="view">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">

                <!-- Modal Header -->
                <div class="modal-header">
                    <h5 class="modal-title">View</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <!-- Modal Body - File Info -->
                <div class="modal-body">
                    <input type="hidden" id="txtID1" />

                    <b>Product Line:</b> <span id="txtProdLine2"></span>, &nbsp;&nbsp;
                    <b>Product Line Code.:</b> <span id="txtRequest2"></span>, &nbsp;&nbsp;
                    <b> Type Name:</b> <span id="txtFileName2"></span>, &nbsp;&nbsp;
                    <b> Type Code:</b> <span id="txtFileTypeCode2"></span>
                    <b>Size: </b><span id="txtSizeCode2"></span>,&nbsp;&nbsp;

                    <b>Sub Type: </b><span id="txtsubTypeCode2"></span>,&nbsp;

                    <b>Code: </b><span id="txtskuTypeCode2"></span>
                    <div class="top10">
                        <img id="displayImage2" class="img-fluid" src="" alt="File Type Image">
                    </div>

                </div>

                <!-- Modal Body - Parameters Table -->
                <div class="modal-body">
                    <h4 class="top10">Parameters</h4>
                    <table class="table table-bordered example1" style="width:100%;" id="tblViewAddedParametersList">
                        <thead>
                            <tr>
                                <th style="width:10%; text-align: center;">Sq. No.</th>
                                <th style="width:40%; text-align: Left;">Process Name</th>
                                <th style="width:40%;  text-align: Left;">Parameter Name</th>
                                <th style="width:10%;  text-align: Left;"><div align="center">Value</div></th>
                                <th style="width:10%;  text-align: Left;"><div align="center">UOM</div></th>
                                <th style="width:10%;  text-align: Left;"><div align="center">Tolerance</div></th>
                            </tr>
                        </thead>
                        <tbody id="ViewAddedParametersList">
                            <!-- Dynamic rows will be injected via JavaScript -->
                        </tbody>
                    </table>
                </div>
                <div class="modal-body">
                    <label class="mt-3">Remark:</label><br>
                    <span id="txtremarklbl"></span>
                </div>
                @* <div class="modal-footer">
					<div class="float-right">

						<button type="button"
								class="btn btn-primary btn-xs btnwidth60" id="btn_DrillTypeApprove"
								title="Approve the selected item"
								onclick="ApproveFileType(document.getElementById('txtID1').value, 5); $('#image').modal('hide');">
							Submit For Approval
						</button>
					</div>
				</div> *@

            </div>
        </div>
    </div>
    <div class="modal fade" id="nonstandard1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">View Sub Type</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="txtID" />

                    <b>Product Line:</b> <span id="txtProdLine"></span>, &nbsp;&nbsp;
                    <b>Product Line Code.:</b> <span id="txtRequest"></span>, &nbsp;&nbsp;
                    <b> Type Name:</b> <span id="txtFileName"></span>, &nbsp;&nbsp;
                    <b> Type Code:</b> <span id="txtFileTypeCode"></span>
                    <b>Size: </b><span id="txtSizeCode"></span>,&nbsp;&nbsp;

                    <b>Sub Type: </b><span id="txtsubTypeCode"></span>,&nbsp;

                    <b>Code: </b><span id="txtskuTypeCode"></span>
                    <div class="top10">
                        <img id="displayImage" class="img-fluid" src="" alt="File Type Image">
                    </div>
</div>
                    <div class="modal-body">
                        <h4 class="top10">Parameters</h4>
                        <table class="table table-bordered example1" style="width:100%;" id="tblViewParametersList">
                            <thead>
                                <tr>
                                    <th style="width:10%; text-align: center;">Sq. No.</th>
                                    <th style="width:40%; text-align: Left;">Process Name</th>
                                    <th style="width:40%;  text-align: Left;">Parameter Name</th>
                                    <th style="width:10%;  text-align: Left;"><div align="center">Value</div></th>
                                    <th style="width:10%;  text-align: Left;"><div align="center">UOM</div></th>
                                    <th style="width:10%;  text-align: Left;"><div align="center">Tolerance</div></th>
                                </tr>
                            </thead>
                            <tbody id="ViewParametersList">
                                <!-- Dynamic rows will be injected via JavaScript -->
                            </tbody>
                        </table>
                    </div>
                    <div class="modal-body">
                        <label class="mt-3">Remark:</label><br>
                    <input type="text" id="txtRemark" class="form-control">
                    </div>

                <div class="modal-footer">
                    <div class="col-lg-2form-group">
                        <div class="">
                            <div class="float-left text-right">
                                <button type="button" id="btn_DrawingReject"
                                        class="btn btn-danger btn-xs btnwidth50"
                                        onclick="UpdateDrawingStatus(document.getElementById('txtID').value, 7); $('#nonstandard1').modal('hide');"
                                        title="Reject the selected item">
                                    Reject
                                </button>

                                <button type="button" id="btn_DrawingApprove"
                                        class="btn btn-success btn-xs btnwidth60"
                                        onclick="UpdateDrawingStatus(document.getElementById('txtID').value, 6); $('#nonstandard1').modal('hide');"
                                        title="Approve the selected item">
                                    Approve
                                </button>
                            </div>
                        </div>
                    </div>
                </div>













            </div>

        </div>
    </div>
    <section class="content-header">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-3">
                    <h5>Drawing</h5>
                </div>
                <div class="col-lg-9">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#" onclick="UserDetailChange('Index')">Home</a></li>
                        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">ECM</a></li>
                        <li class="breadcrumb-item active">Masters</li>
                         <li class="breadcrumb-item active">Cutting Tools</li>

                        <li class="breadcrumb-item active">Drawing</li>

                    </ol>
                </div>
            </div>

            <div class="row">
                <div class="col-lg-12">
                    <div class="btnwidth50 float-right ">
                        <button type="button" class="btn btn-block bg-gradient-danger btn-xs float-left"
                                onclick="history.back()" id="btnback" title="Go back to the previous page">
                            Back
                        </button>
                    </div>

                </div>
            </div>
        </div><!-- /.container-fluid -->

    </section>

    <section class="content">

        <div class="container-fluid">

            <div class="row">
                <div class="col-md-12">
                    <div class="card card-outline card-primary">
                        <div class="card-header">
                            <h3 class="card-title">New Requests</h3>

                            <div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse">
                                    <i class="fas fa-minus"></i>
                                </button>
                            </div>
                            <!-- /.card-tools -->
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-bordered example1">
                                    <thead>

                                        <tr>

                                            <th width="10%"><div align="center">Request No.</div></th>
                                            <th width="11%"><div align="left"> Product Line</div></th>
                                            <th width="15%"><div align="center">Production SKU</div></th>
                                            <th width="28%"><div align="left">Description</div></th>
                                            <th width="12%"><div align="center">Drawing</div></th>
                                            <th width="12%"><div align="center">Add Operation</div></th>
                                            <th width="22.5%"><div align="center"></div></th>
                                        </tr>
                                    </thead>
                                    <tbody>

                                        
                                        @foreach (var item in Model.DrawingDetails.Where(x => x.GridNo == 1))

                                        
                                           
                                            {
                                                <tr>

                                                    <td>
                                                        <div align="center">@item.Id</div>
                                                    </td>

                                                    <td>
                                                        <div align="left">@item.ProductLine</div>
                                                    </td>

                                                    <td>
                                                        <div align="center">
                                                            <a href="#" title="View Parameters"
                                                               data-toggle="modal"
                                                            onclick="ModelFileView('@item.Id');">
                                                                @item.SKUCode
                                                            </a>
                                                        </div>
                                                    </td>

                                                    <td>
                                                        <div align="left">@item.SKUDesc</div>
                                                    </td>

                                                    <td>
                                                    <div align="center">
                                                        <a onclick="URLReport(@item.Id)" id="btn_DrawingGenerate"
                                                           class="btn btn-primary btn-xs">Generate</a>
                                                    </div>
                                                    </td>

                                                    <td>
                                                        <div align="center">
                                                            <button type="button"
                                                                    data-toggle="modal"
                                                                    data-target="#nonstandard3"
                                                                    class="btn btn-primary btn-xs btnwidth50" disabled>
                                                                Add
                                                            </button>
                                                        </div>
                                                    </td>

                                                    <td>
                                                        <div align="center">
                                                            <button type="button"
                                                                    class="btn btn-primary btn-xs btnwidth95"
                                                                    onclick="UpdateDrawingStatus(@item.Id,5);">
                                                                Submit for Approval
                                                            </button>
                                                        </div>
                                                    </td>

                                                </tr>
                                            }
                                        

                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <!-- /.card -->
                </div>
            </div>

            <div class="row">
                <div class="col-md-12">
                    <div class="card card-outline card-warning collapsed-card">
                        <div class="card-header">
                            <h3 class="card-title">Requests for Approval</h3>

                            <div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse">
                                    <i class="fas fa-plus"></i>
                                </button>
                            </div>
                            <!-- /.card-tools -->
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <table class="table table-bordered example1">
                                <thead>

                                    <tr>

                                        <th width="7%"><div align="center">Request No.</div></th>
                                        <th width="8%"><div align="left"> Product Line</div></th>
                                        <th width="8%"><div align="center">Production SKU</div></th>
                                        <th width="45%"><div align="left">Description</div></th>
                                        <th width="7%"><div align="center">Operation</div></th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @if (Model.DrawingDetails != null)
                                    {
                                        foreach (var item in Model.DrawingDetails.Where(x => x.GridNo == 2))
                                        {
                                            <tr>

                                                <td>
                                                    <div align="center">
                                                       
                                                            @item.Id
                                                      
                                                    </div>
                                                </td>

                                                <td>
                                                    <div align="left">@item.ProductLine</div>
                                                </td>

                                                <td>
                                                    <div align="center">
                                                        <a href="#" title="View Parameters"
                                                           data-toggle="modal" onclick="ModelDrawingView('@item.Id');"
                                                           data-target="#nonstandard1">
                                                            @item.SKUCode
                                                        </a>
                                                    </div>
                                                </td>

                                                <td>
                                                    <div align="left">@item.SKUDesc</div>
                                                </td>

                                                <td>
                                                    <div align="center">
                                                        <button type="button"
                                                                data-toggle="modal"
                                                                data-target="#nonstandard4"
                                                                class="btn btn-primary btn-xs btnwidth50" disabled>
                                                            View
                                                        </button>
                                                    </div>
                                                </td>

                                            </tr>
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <!-- /.card -->
                </div>
            </div>



            <div class="row">
                <div class="col-md-12">
                    <div class="card card-outline card-success collapsed-card">
                        <div class="card-header">
                            <h3 class="card-title">Drawing Master</h3>

                            <div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse">
                                    <i class="fas fa-plus"></i>
                                </button>
                            </div>
                            <!-- /.card-tools -->
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <table class="table table-bordered example1">
                                <thead>

                                    <tr>

                                        <th width="7%"><div align="center">Request No.</div></th>
                                        <th width="8%"><div align="left"> Product Line</div></th>
                                        <th width="8%"><div align="center">Production SKU</div></th>
                                        <th width="45%"><div align="left">Drwaing Description</div></th>
                                        <th width="7%"><div align="center">Drwaing PDF</div></th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @if (Model.DrawingDetails != null)
                                    {
                                        foreach (var item in Model.DrawingDetails.Where(x => x.GridNo == 3))
                                        {
                                            <tr>

                                                <td>
                                                    <div align="center">@item.Id</div>
                                                </td>

                                                <td>
                                                    <div align="left">@item.ProductLine</div>
                                                </td>

                                                <td>
                                                    <div align="center">@item.SKUCode</div>
                                                </td>

                                                <td>
                                                    <div align="left">@item.SKUDesc</div>
                                                </td>

                                                <td>
                                                    <div align="center">
                                                        <button type="button"
                                                                class="btn btn-primary btn-xs"
                                                                onclick="URLReport('@item.Id')"
                                                                 id="btn_DownloadDrawingGenerate"
                                                                title="Download the file">
                                                            Download
                                                        </button>
                                                    </div>
                                                </td>

                                            </tr>
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <!-- /.card -->
                </div>
            </div>
            </div>

    </section>

    <!-- InstanceEndEditable -->
</div>
<script>
    function ModelFileView(TypeId) {

        let json = {
            TxnId: TypeId
        };

        $.ajax({
            type: "POST",
            url: '@Url.Action("CT_GetDrawingListById", "DrillMaster")',
            data: { JsonData: JSON.stringify(json) },
            dataType: "json",
            beforeSend: function () { $('#loader').show(); },
            complete: function () { $('#loader').hide(); },

            success: function (res) {

                if (!res || !res.response || res.response.length === 0) {
                    alert("No data found");
                    return;
                }

                let data = res.response[0];

                //--------------------------------
                // Bind header fields
                //--------------------------------

                $("#txtID1").val(data.typeDetailsRowId);

                $("#txtProdLine2").text(data.productLine);

                $("#txtRequest2").text(data.productLineCode);

                $("#txtFileName2").text(data.ctTypeName);

                $("#txtFileTypeCode2").text(data.ctTypeCode);

                $("#txtRequest2").text(data.productLineCode);

                $("#txtFileName2").text(data.ctTypeName);

                $("#txtFileTypeCode2").text(data.ctTypeCode);
                  $("#txtskuTypeCode2").text(data.skuCode);

                $("#txtsubTypeCode2").text(data.subType);

                $("#txtSizeCode2").text(data.sizeType);
                  
                $("#txtremarklbl").text(data.remark || "-");
                var imageFileName = data.imageFileName;
                var productLine =	data.productLine;
                //--------------------------------
                // Bind image
                //--------------------------------

                // if (data.imageFileName) {
                // 	$("#displayImage").attr(
                // 		"src",
                // 		"/others/ct/types/" + data.imageFileName
                // 	);
                // }

                //--------------------------------
                // Bind Parameters Table
                //--------------------------------
                if ($.fn.DataTable.isDataTable('#tblViewAddedParametersList')) {
                    $('#tblViewAddedParametersList').DataTable().clear().destroy();
                }

                //--------------------------------
                // Initialize DataTable
                //--------------------------------

                      var dt = $('#tblViewAddedParametersList').DataTable({
        responsive: true,
        paging: true,
        pageLength: 10,
        searching: true,
        ordering: true,
        info: true,
        autoWidth: false,

        // ✅ CUSTOM LAYOUT
        dom: "<'row'<'col-sm-2'l><'col-sm-6 text-center'B><'col-sm-4'f>>" +
             "<'row'<'col-sm-12'tr>>" +
             "<'row'<'col-sm-5'i><'col-sm-7'p>>",

        buttons: [
            {
                extend: 'csv',
                text: 'CSV',
                className: 'btn btn-secondary'
            }
        ],

        columnDefs: [
            { className: "dt-center", targets: "_all" }
        ]
    });
                $("#ViewAddedParametersList").empty();

                
                if (data.csvData && data.csvData.length > 0) {

                       for (var i = 0; i < data.csvData.length; i++) {

        var p = data.csvData[i];

        rows = [
            '<div align="center">' + p["Sq. No."] + '</div>',
            '<div align="left">' + p["Process Name"] + '</div>',
            '<div align="left">' + p["Parameter Name"] + '</div>',
            '<div align="center">' + p.uom+ '</div>',
            '<div align="center">' + p.value + '</div>',
            '<div align="center">' + p.tolerance + '</div>'
        ];

        dt.row.add(rows).draw(false);
    }
                }
                //--------------------------------
                // Show modal
                //--------------------------------
                OpenDocument(imageFileName,'displayImage2',productLine);
    // 			$('#image').modal('show');
                $("#view").modal("show");
            },

            error: function () {
                alert("Error loading data");
            }
        });
    }
     
    function ModelDrawingView(TypeId) {

        let json = {
            TxnId: TypeId
        };

        $.ajax({
            type: "POST",
            url: '@Url.Action("CT_GetDrawingListById", "DrillMaster")',
            data: { JsonData: JSON.stringify(json) },
            dataType: "json",
            beforeSend: function () { $('#loader').show(); },
            complete: function () { $('#loader').hide(); },

            success: function (res) {

                if (!res || !res.response || res.response.length === 0) {
                    alert("No data found");
                    return;
                }

                let data = res.response[0];

                //--------------------------------
                // Bind header fields
                //--------------------------------

                $("#txtID").val(data.id);

                $("#txtProdLine").text(data.productLine);

                $("#txtRequest").text(data.productLineCode);

                $("#txtFileName").text(data.ctTypeName);

                $("#txtFileTypeCode").text(data.ctTypeCode);

                $("#txtRequest").text(data.productLineCode);

                $("#txtFileName").text(data.ctTypeName);

                $("#txtFileTypeCode").text(data.ctTypeCode);
                  $("#txtskuTypeCode").text(data.skuCode);

                $("#txtsubTypeCode").text(data.subType);

                $("#txtSizeCode").text(data.sizeType);

                $("#txtremark").text(data.remark || "-");
                var imageFileName = data.imageFileName;
                var productLine =	data.productLine;
                //--------------------------------
                // Bind image
                //--------------------------------

                // if (data.imageFileName) {
                // 	$("#displayImage").attr(
                // 		"src",
                // 		"/others/ct/types/" + data.imageFileName
                // 	);
                // }

                //--------------------------------
                // Bind Parameters Table
                //--------------------------------
                if ($.fn.DataTable.isDataTable('#tblViewParametersList')) {
                    $('#tblViewParametersList').DataTable().clear().destroy();
                }

                //--------------------------------
                // Initialize DataTable
                //--------------------------------

                
                          var dt = $('#tblViewParametersList').DataTable({
        responsive: true,
        paging: true,
        pageLength: 10,
        searching: true,
        ordering: true,
        info: true,
        autoWidth: false,

        // ✅ CUSTOM LAYOUT
        dom: "<'row'<'col-sm-2'l><'col-sm-6 text-center'B><'col-sm-4'f>>" +
             "<'row'<'col-sm-12'tr>>" +
             "<'row'<'col-sm-5'i><'col-sm-7'p>>",

        buttons: [
            {
                extend: 'csv',
                text: 'CSV',
                className: 'btn btn-secondary'
            }
        ],

        columnDefs: [
            { className: "dt-center", targets: "_all" }
        ]
    });
              $("#ViewParametersList").empty();

                // if (data.typeParamsJarr && data.typeParamsJarr.length > 0) {

                // 	let html = "";

                // 	$.each(data.typeParamsJarr, function (i, item) {

                // 		html += `
                // 			<tr>
                // 				<td style="text-align:center;">${item.processSequence}</td>
                // 				<td>${item.processName}</td>
                // 				<td>${item.paramName}</td>
                // 				<td>${item.paramUOM}</td>
                // 			</tr>`;
                // 	});

                // 	$("#showAddedParametersList").html(html);
                // }
                if (data.csvData && data.csvData.length > 0) {

                       for (var i = 0; i < data.csvData.length; i++) {

        var p = data.csvData[i];

        rows = [
            '<div align="center">' + p["Sq. No."] + '</div>',
            '<div align="left">' + p["Process Name"] + '</div>',
            '<div align="left">' + p["Parameter Name"] + '</div>',
            '<div align="center">' + p.uom+ '</div>',
            '<div align="center">' + p.value + '</div>',
            '<div align="center">' + p.tolerance + '</div>'
        ];

        dt.row.add(rows).draw(false);
    }
                }
                //--------------------------------
                // Show modal
                //--------------------------------
                OpenDocument(imageFileName,'displayImage',productLine);
    // 			$('#image').modal('show');
                $("#nonstandard1").modal("show");
            },

            error: function () {
                alert("Error loading data");
            }
        });
    }
     
    
    function UpdateDrawingStatus(DrawingId, StatusID, SKUKey) {





        var Remark = $('#txtRemark').val();
        var iLoggedInUserID = parseInt('@Context.Session.GetInt32("CreatedBy")');

        let jsonData = { ID: DrawingId, StatusID: StatusID, CreatedBy: iLoggedInUserID, Remark: Remark };
        var JSONInformation = JSON.stringify(jsonData);
        $.ajax({
            type: 'POST',
            url: '@Url.Action("UpdateDrawingStatus", "Master")',
            data: { JSONData: JSONInformation },
            dataType: 'json',
            success: function (data) {
                alert(data.response);
                window.location.reload();
            },
            error: function (err) {
                alert(err.statusText);
            }
        });
    }
    function RequestApprovalRejection(flag) {
        var Remark = $("#txtRemark").val();

        var iLoggedInUserID = parseInt('@Context.Session.GetInt32("CreatedBy")');
        var StatusID ;

        if (flag == 0)
            StatusID = 4;
        else
            StatusID = 3;

        var json = { ID: currentDrawingId, StatusID: StatusID, CreatedBy: iLoggedInUserID, Remark: Remark };

        var JSONInsert = JSON.stringify(json);
        $.ajax({
            type: 'POST',
            url: '@Url.Action("UpdateDrawingStatus", "Master")',
            data: { JsonData: JSONInsert },
            datatype: 'json',
            success: function (data) {
                alert(data.response);
                window.location.reload();
            },
            error: function () {
                alert('Error occurred while processing your request.');
            }
        });



    }


     function URLReport(RequestId) {
        var ReportBasePath = '@ViewBag.ReportBasePath';
        var url1 = 'CTDrawingGeneration_ecm.aspx?';
        var url = 'ID=';
        var fullUrl = ReportBasePath + url1 + url + RequestId;
        window.open(fullUrl, '_blank');
    }
    async function OpenDocument(FileName, ImageId,ProdLine) {
       var JwToken = '@ViewBag.JWTToken';
       var blobStorageUrl = '@ViewBag.ApiUrl';
       let blobPathPrefix = '@ViewBag.BaseBlobPath';
        //var path = 'others\\filetypes' + '\\';
    var path = 'others\\ct\\types\\';
        const blobpath = (normalizePathPrefix(blobPathPrefix) + normalizePathPrefix(path) ).toLowerCase()+ FileName;

            const JSONData = {
                blobPath: blobpath,
                asSas: false,
                downloadFileName: FileName
            };

            try {
                $('#loader').show();

                const response = await $.ajax({
                    url: blobStorageUrl + 'Files/get',
                    headers: { Authorization: 'Bearer ' + JwToken },
                    type: "POST",
                    contentType: "application/json",
                    data: JSON.stringify(JSONData),
                    xhrFields: { responseType: 'blob' }
                });

                if (response && response.size > 0) {
                    const objectURL = URL.createObjectURL(response);
                    // Display the image in the img tag
                    const imgElement = document.getElementById(ImageId);
                imgElement.src = objectURL;
                }
            }
            catch (error) {
                console.error("Error:", error);
                const imgElement = document.getElementById(ImageId);
                imgElement.src = '';

            }
            finally {
             $('#loader').hide();
            }
    }
        async function OpenReport(FileName) {
                   var JwToken = '@ViewBag.JWTToken';
       var blobStorageUrl = '@ViewBag.ApiUrl';
       let blobPathPrefix = '@ViewBag.BaseBlobPath';
            var path = 'drawings' + '\\';

             const blobpath = (normalizePathPrefix(blobPathPrefix) + normalizePathPrefix(path) ).toLowerCase()+ FileName;

                const JSONData = {
                    blobPath: blobpath,
                    asSas: false,
                    downloadFileName: FileName
                };

                try {
                    $('#loader').show();

                    const response = await $.ajax({
                        url: blobStorageUrl + 'Files/get',
                        headers: { Authorization: 'Bearer ' + JwToken },
                        type: "POST",
                        contentType: "application/json",
                        data: JSON.stringify(JSONData),
                        xhrFields: { responseType: 'blob' }
                    });

                    if (response && response.size > 0) {
      //  const blob = new Blob([response], { type: 'application/pdf' });
      //const blob = await response.blob();
      const objectURL = URL.createObjectURL(response);


        // Open the PDF in a new tab
        const newTab = window.open(objectURL, '_blank');
                //	const newTab = window.open(objectURL, '_blank');

                // Add a download button in the new tab
                if (newTab) {
                    newTab.onload = () => {
                        const downloadButton = newTab.document.createElement('a');
                        downloadButton.href = objectURL;
                        downloadButton.download = FileName; //Set the original file name
                        downloadButton.innerText = "Download PDF";
                        downloadButton.style.cssText = "position: fixed; top: 10px; right: 10px; background: #007bff; color: #fff; padding: 10px; text-decoration: none; border-radius: 5px;";
                        newTab.document.body.appendChild(downloadButton);
                    };
                }

                //Revoke the object URL after some time (optional cleanup)
                setTimeout(() => URL.revokeObjectURL(objectURL), 60000);
        // Force the browser's "Save As" name when user clicks download in viewer
        // (This works in Chrome, Edge, and most modern browsers)
        // if (newTab) {
        //     newTab.document.title = FileName;
        //     const a = document.createElement('a');
        //     a.href = objectURL;
        //     a.download = FileName; // Sets the default filename for download
        //     newTab.document.body.appendChild(a);
        // }

        // // Cleanup after some time
        // setTimeout(() => URL.revokeObjectURL(objectURL), 60000);
    }

                // Revoke the object URL after some time (optional cleanup)
                //setTimeout(() => URL.revokeObjectURL(objectURL), 60000); // 60 seconds

                }
                catch (error) {
                    console.error("Error:", error);
                }
                finally {
                 $('#loader').hide();
                }
        }
</script>